<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade in col-md-4">
    <button class="close" data-dismiss="alert">×</button>
    <strong><%= message %></strong>
  </div>
<% end %>